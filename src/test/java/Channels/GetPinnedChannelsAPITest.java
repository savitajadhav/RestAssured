package Channels;

import java.io.IOException;
import org.testng.annotations.Test;
import com.adweb.qa.base.TestBase;
import OAuth.LoginAPITest;

public class GetPinnedChannelsAPITest{
	/*
	 * public GetPinnedChannelsAPITest() throws IOException { super(); }
	 * 
	 * // Test Case1: Verify that API returns the list all existing channels of the
	 * // application.
	 * 
	 * @Test public void getPinnedChannelListTest() throws IOException {
	 * 
	 * LoginAPITest login = new LoginAPITest(); login.loginTest("iphone",
	 * "iphone@123"); System.out.
	 * println("Test Case1: Verify that API returns the list of all pinned channels."
	 * ); log.info("Get all channel request execution started");
	 * Login.getLoginResponse(); Channel.getChannel();
	 * log.info("Get all channel request execution ended"); }
	 * 
	 * // Test Case2: Verify that API returns the correct status code
	 * 
	 * @Test public static void verifyStatusCode() { log.
	 * info("Test Case2: Verify that API returns the correct status code for 'Get All Channel List' request "
	 * ); Utility.assertEquals(Channel.statusCode, RESPONSE_STATUS_CODE_200);
	 * 
	 * }
	 */

}
